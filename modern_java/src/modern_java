모던 자바의 큰 특징
    -스트림 처리
        :스트림 내의 요소를 쉽게 병렬로 처리할 수 있는 환경을 제공한다.
    -동작 파라미터화로 메서드에 코드 전달
    -디폴트 메서드와 자바모듈
        :구현 클래스에서 구현하지 않아도 되는 메서드를 인터페이스에 추가할 수 있는 기능.
         기존의 코드를 건드리지 않고도 원래의 인터페이스 설계를 자유롭게 확장할 수 있다.
         인터페이스 규격명세에 default라는 새로운 키워드를 지원
    -NullPointer 예외를 피할 수 있도록 도와주는 Optional<T> 클래스 제공
    -패턴 매칭이 완벽하게 지원되지 않아 스칼라 프로그래밍 언어로 패턴 매칭을 사용
     (스칼라 expr match는 자바의 switch(expr)과 같은 기능 수행)

predicate : 수학에서 인수로 값을 받아 true 나 false를 반환하는 함수



