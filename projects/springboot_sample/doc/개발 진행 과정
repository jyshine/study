1.의존성 추가
    -SpringApplication 만들어 빈페이지 Test
2.H2 데이터베이스 설치
    -해당 경로로 이동해서 ./h2.sh
3.스프링 데이터 JPA와 DB 설정 및 동작 확인
    -application.yml 설정 파일 추
    -entity 클래스 만든 후 test에서 객체 생성을 통해 쿼리문 확인
    -repository > interface 생성 후 상속 받아 JpaRepository<, > 테스트

4.Entity 생성




